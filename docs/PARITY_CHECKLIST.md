# Mindset v1 -> v2 Parity Checklist

Status key: `DONE`, `PARTIAL`, `TODO`

## Core Tracking

1. Automatic visit tracking on normal web page loads: `DONE`
2. Manual runtime track action support: `DONE`
3. Weekly summary aggregation: `DONE`

## Popup UX

1. Health/status indicator: `DONE`
2. Weekly metrics (visits/domains/credibility): `DONE`
3. Dashboard navigation from popup: `DONE`
4. Rich insight copy and recommendation actions: `DONE`

## Dashboard UX

1. Weekly score cards: `DONE`
2. Category distribution visibility: `DONE`
3. Recent visits table: `DONE`
4. Historical trends and filters: `DONE`
5. Export/share/report actions: `DONE`

## Interventions

1. Low-credibility intervention banner: `DONE`
2. Same-story upgrade path: `PARTIAL`
3. Echo-chamber breaker flow: `PARTIAL`
4. Credibility recovery loop UI states: `DONE`

## Settings and Controls

1. Core settings UI: `DONE`
2. Guidance mode tuning: `DONE`
3. Privacy/data controls: `DONE`

## Migration and Reliability

1. v1 payload migration to v2 schema: `DONE`
2. CI test gate + unsafe render pattern guard: `DONE`
3. Browser automation depth beyond Node e2e flow: `DONE`
